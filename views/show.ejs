<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="styles.css">
    <title>Welcome to show</title>
</head>
<body>
    <nav><a class="btn btn-primary" href="/index"> Go back</a></nav>

    <div class="show-container text-center ">
    <h1>show</h1>

    <div class="yourPosts">
        <!-- userAccount is the whole user object -->
        <!-- just found out they are giving me errors but the logic still runs. 
            just stops the styles from working on page. so bootstrap -->
<% if (userAccount.posts.length==0) { %>

    <p>You have no posts to show </p>
 
<% }  else{ %>
<div >
    <p>Here are your posts</p>
</div>
    <% for ( let i= userAccount.posts.length-1; i>=0; i-- ){ %>

      
       <div class="singlePost shadow p-3  rounded p-3 mb-2 bg-info text-white"><%=   userAccount.posts[i].content%>  
        
    </div>

<%} }%>

    </div>
</div>
    
</body>
</html>