<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="styles.css">
    <title>Sign In</title>
</head>
<body>
    
<div class="container-for-login ">
    <h1 >Welcome, please sign in</h1>
    <!-- flash messages for different scenarios. -->
    <div > <p class="error"><%= fail %>  <%= exists %>   <%= blank %>  <%= cpuLoggedYouOut %></p></div> 
     <div class="newAccount">
        <%= newA %>
    </div>
    <h3>Create a user </h3>

    <div class="form-wrapper">
    <form name="createForm" action="/create/user"  method="POST" class="validateForm" novalidate  >
        <div class="form-row">
       <label for="createFormUser"> Username:</label>  
       <input type="text" class="form-control" name="user" id="createFormUser" required> 
        </div>
        <div>
        <label for="createUserPassword"> Password:</label>
         <input type="password" class="form-control" name="password"  required >
        </div>
        <input type="submit" name="submit" class="btn btn-primary" required >
    </form>

    <h3>or login</h3>
    <form name="userForm"  class="userForm validateForm" action="/login/user"  method="POST"  novalidate >
        <div class="form-row">
       <label for="userFormName"> Username:</label>  <input class="form-control" type="text" name="user" id=" userFormName" required> 
    </div>
    <div>
        <label for="userFormPassword"> Password:</label>  <input class="form-control" type="password" name="password" id="userFormPassword" required>
        <input type="submit" class="btn btn-primary" name="submit"  required >
    </div>
    </form>
</div>
</div>


<script>
    (function() {
  'use strict';
  window.addEventListener('load', function() {
    // Fetch all the forms we want to apply custom Bootstrap validation styles to
    let forms = document.getElementsByClassName('validateForm');
    // Loop over them and prevent submission
    let validation = Array.prototype.filter.call(forms, function(form) {
      form.addEventListener('submit', function(event) {
        if (form.checkValidity() === false) {
          event.preventDefault();
          event.stopPropagation();
        }
        form.classList.add('was-validated');
      }, false);
    });
  }, false);
})();

</script>
</body>
</html>

    <!-- // THE WAY TO MAKE SURE MY FORM WAS GOOD USING VANILLA JS . GOT FROM W3 SCHOOLS -->
<!-- <script>

    //  onsubmit="return validateCreate()"
    // onsubmit="return validateUser()"

//    both do the same thing for diff forms. prevent blank entries from w3 schools
// when I tried doing an event listener w the same code it did not work.
   function validateCreate() {
  let x = document.forms["createForm"]["user"].value.trim();
  if (x == "") {
    alert("Name must be filled out");
    return false;
  }
}

function validateUser() {
  let x = document.forms["userForm"]["user"].value.trim();
  if (x == "") {
    alert("Name must be filled out");
    return false;
  }
}

</script> -->
    