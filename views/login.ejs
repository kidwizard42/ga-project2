<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <title>Sign In</title>
</head>
<body>
    <h1 >Welcome, please sign in</h1>

    <!-- flash messages for different scenarios. -->
    <div class="error"><%= fail %>  <%= exists %>   <%= blank %>  <%= cpuLoggedYouOut %></div> 
     <div class="newAccount">
        <%= newA %>
    </div>
    <h3>Create a user </h3>
    <form name="createForm" action="/create/user" onsubmit="return validateCreate()" method="POST" >
        <div>
       <label for="createFormUser"> Username:</label>  
       <input type="text" name="user" id="createFormUser"> 
        </div>
        <div>
        <label for="createUserPassword"> Password:</label>
         <input type="password" name="password" id="">
        </div>
        <input type="submit" name="submit" class="btn btn-primary">
    </form>

    <h3>or login</h3>
    <form name="userForm"  class="userForm" action="/login/user" onsubmit="return validateUser()" method="POST">
        <div>
       <label for="userFormName"> Username:</label>  <input type="text" name="user" id=" userFormName"> 
    </div>
    <div>
        <label for="userFormPassword"> Password:</label>  <input type="password" name="password" id="userFormPassword">
        <input type="submit" name="submit" id="">
    </div>
    </form>

<script>
//    both do the same thing for diff forms. prevent blank entries from w3 schools
// when I tried doing an event listener w the same code it did not work.
   function validateCreate() {
  let x = document.forms["createForm"]["user"].value.trim();
  if (x == "") {
    alert("Name must be filled out");
    return false;
  }
}

function validateUser() {
  let x = document.forms["userForm"]["user"].value.trim();
  if (x == "") {
    alert("Name must be filled out");
    return false;
  }
}

</script>
    
</body>
</html>